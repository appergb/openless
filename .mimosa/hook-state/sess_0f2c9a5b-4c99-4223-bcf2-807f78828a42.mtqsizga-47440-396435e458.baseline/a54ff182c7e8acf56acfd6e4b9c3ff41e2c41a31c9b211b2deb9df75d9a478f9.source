# OpenLess repository guide

## 当前项目

`1-app` 是独立 Git 仓库，应用工作目录为 `openless-all/app`。默认开发主线为 `beta`，`main` 为稳定发布；PR 面向 `beta`。本地入口只有本文件，详细信息由 [docs/index.md](docs/index.md) 路由，不递归读取兄弟项目或全部文档。

## 技术与边界

- Core 2.0：React/TypeScript/i18next 界面、Tauri 2 Windows/macOS Host、Rust `crates/openless-core`，Linux 使用已有 egui/eframe Host/UI 起点。
- 先读 [2.0 当前范围](docs/2.0-requirements.md)与[架构](docs/architecture.md)。IPC 改动须同步 Rust、typed wrapper 和必要合同测试；平台特有内容按任务选择。
- Windows/macOS 完整交付范围与 Linux 接入/产品验收分开；能力接口、构建与真实设备可用性分别报告。不要用配置状态伪造就绪或成功。
- 不覆盖用户其他修改，不打印密钥，不修改其他项目或生产配置。业务规则属于 Core，UI 不另写一套。

## 当前意图与活动计划

用户授权用云端最新已合并主线替换旧分支、删除未提交修改，梳理2.0并优化入口/设置/布局，为Linux提供引导。界面整理与追加视觉改进已完成，已提交 `30e580fc` 并合并进本地 `beta`（合并 `11d9aa4b`，含云端新增 4 提交至 `82aa33ff`，零冲突；合并结果 `npm test` 构建与测试通过）。`feat/core-2-ux-linux` 已删除，过期 worktree 已清理，其余本地分支仅保留含独立提交或备份性质者；`beta` 领先 `origin/beta` 2 个提交、未推送。原生 Linux 验收继续按交接清单执行。[本次计划与进度](docs/plans/active/2026-09-06-core-2-ux.md)、[体验梳理与建议](docs/2.0-ux-review.md)。

## 验证和完成

在应用目录执行 `npm test`（含构建及Core快捷键回归）。Rust/Core/Linux按架构文档选择实际测试；macOS测试不能替代Linux目标编译或设备交互。复杂改动须验证并同步文档，简单文字/布局小修不另造测试。新增规范或外部知识只保留一个来源，文档标明状态、版本和验证阶段。
